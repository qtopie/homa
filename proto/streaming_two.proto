syntax = "proto3";

package main;

// Define the second streaming service
service StreamingServiceTwo {
  rpc StreamDataTwo(StreamRequest) returns (stream StreamResponse);
}

message StreamRequest {
  string message = 1;
}

message StreamResponse {
  string data = 1;
}